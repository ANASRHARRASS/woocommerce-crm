<?php
/**
 * (Intentionally disabled)
 * This file formerly duplicated the main plugin bootstrap.
 * Kept only as a stub to avoid accidental re‑addition.
 */
if ( ! defined( 'ABSPATH' ) ) {
    exit;
}
return; // No-op to prevent a second plugin instance.

// Removed (legacy duplicate bootstrap) – keep file deleted or empty to avoid re‑adding.